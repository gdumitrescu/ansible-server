---
language: python
python: '2.7'

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq python-apt

install:
  - pip install ansible==2.7.5

script:
  - ansible-playbook -i hosts setup.yml --syntax-check
  - ansible-playbook -i hosts setup.yml --connection=local --sudo
